> BACKEND:

> to install all the dependencies
> npm install

> to run the server
> nodemon app

# incase you're getting error even after following the above steps:
# rm -rf node_modules
# npm install
